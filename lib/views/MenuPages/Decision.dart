import 'package:flutter/material.dart';
class DecisionSupport extends StatelessWidget {
  const DecisionSupport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Decision Support'),
    );
  }
}