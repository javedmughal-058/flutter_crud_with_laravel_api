import 'package:flutter/material.dart';

class EmployeeSelfService extends StatelessWidget {
  const EmployeeSelfService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Appointment'),
    );
  }
}
