import 'package:flutter/material.dart';

class CTM extends StatelessWidget {
  const CTM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('CTM'),
    );
  }
}
