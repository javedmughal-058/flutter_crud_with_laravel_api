import 'package:flutter/material.dart';

class GarmentRequest extends StatelessWidget {
  const GarmentRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Garments'),
    );
  }
}
