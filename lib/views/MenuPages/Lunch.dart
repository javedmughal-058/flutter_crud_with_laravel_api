import 'package:flutter/material.dart';

class LunchRequest extends StatelessWidget {
  const LunchRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Lunch'),
    );
  }
}
